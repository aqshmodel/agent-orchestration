
import { ja } from './ja';
import { en } from './en';
import { TranslationResource } from '../types';

export const translations: { ja: TranslationResource; en: TranslationResource } = {
  ja,
  en,
};
